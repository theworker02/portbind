{
  "name": "portbind",
  "version": "0.1.0",
  "description": "Scan files for 0.0.0.0 and PORT/$PORT bind patterns.",
  "bin": {
    "portbind": "./src/cli.js"
  },
  "main": "./src/index.js",
  "scripts": {
    "test": "node --test"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
